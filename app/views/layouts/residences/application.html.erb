<!DOCTYPE html>
<html>
  <head>
    <title>ManagementApp</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <h1> residences </h1>
    <% if residence_signed_in? %>
      <%= "LogOnUser: #{current_residence.email}" %>
      <%= link_to 'SignOut', destroy_residence_session_path, method: :delete %>
    <% else %>
    <%= link_to 'SingUp', new_residence_registration_path %>
    <%= link_to 'SignIn', new_residence_session_path %>
    <%= yield %>
  </body>
</html>