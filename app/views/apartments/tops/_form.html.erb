<%= form_with(model: apartments_top, local: true) do |form| %>
  <% if apartments_top.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(apartments_top.errors.count, "error") %> prohibited this apartments_top from being saved:</h2>

      <ul>
        <% apartments_top.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
