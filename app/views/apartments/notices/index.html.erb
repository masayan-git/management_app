<p id="notice"><%= notice %></p>

<h1>Apartments Notices</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @apartments_notices.each do |notice| %>
      <tr>
        <td><%= notice.title%>
        <td><%= link_to 'Show', apartments_notice_path(notice.id) %></td>
        
      </tr>
    <% end %>
  </tbody>
</table>



