<p id="notice"><%= notice %></p>

<h1>Apartments Articles</h1>

<table>
  <thead>
    <tr>
      <th colspan="3">Title</th>
    </tr>
  </thead>

  <tbody>
    <% @apartments_articles.each do |apartments_article| %>
      <tr>
        <td><%= apartments_article.title%></td>
        <td><%= link_to 'Show', apartments_article %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Apartments Article', new_apartments_article_path %>
