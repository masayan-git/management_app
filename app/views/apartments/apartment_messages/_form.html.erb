<%= form_with(model: apartments_apartment_message, local: true) do |form| %>
  <% if apartments_apartment_message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(apartments_apartment_message.errors.count, "error") %> prohibited this apartments_apartment_message from being saved:</h2>

      <ul>
        <% apartments_apartment_message.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
