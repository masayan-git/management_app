<%= form_with(model: residences_residence_message, local: true) do |form| %>
  <% if residences_residence_message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(residences_residence_message.errors.count, "error") %> prohibited this residences_residence_message from being saved:</h2>

      <ul>
        <% residences_residence_message.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
