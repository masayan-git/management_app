<p id="notice"><%= notice %></p>

<h1>Residences Notices</h1>

<table>
  <thead>
    <tr>
      <th colspan="3">お知らせ</th>
    </tr>
  </thead>

  <tbody>
    <% @residences_notices.each do |notice| %>
      <tr>
        <td><%= notice.title%></td>
        <td><%= link_to 'Show', residences_notice_path(notice.id) %></td>
        
      </tr>
    <% end %>
  </tbody>
</table>

<br>


