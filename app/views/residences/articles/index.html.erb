<p id="notice"><%= notice %></p>

<h1>Residences Articles</h1>

<table>
  <thead>
    <tr>
      <th colspan="3">Title</th>
    </tr>
  </thead>

  <tbody>
    <% @residences_articles.each do |residences_article| %>
      <tr>
        <td><%= residences_articles.title%></td>
        <td><%= link_to 'Show', residences_article %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Residences Article', new_residences_article_path %>
