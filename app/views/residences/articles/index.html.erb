<p id="notice"><%= notice %></p>

<h1>Residences Articles</h1>

<table>
  <thead>
    <tr>
      <th colspan="3">Title</th>
    </tr>
  </thead>

  <tbody>
    <% @residences_articles.each do |article| %>
      <tr>
        <td><%= article.title%></td>
        <td><%= link_to 'Show', residences_article_path(article.id) %></td>
      </tr>
    <% end %>
  </tbody>
</table>


