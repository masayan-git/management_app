<%= form_with(model: masters_top, local: true) do |form| %>
  <% if masters_top.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(masters_top.errors.count, "error") %> prohibited this masters_top from being saved:</h2>

      <ul>
        <% masters_top.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
