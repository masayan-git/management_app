<p id="notice"><%= notice %></p>

<h1>Masters Tags</h1>

<table>
  <thead>
    <tr>
      <th colspan="3">タグの名前</th>
    </tr>
  </thead>

  <tbody>
    <% @masters_tags.each do |masters_tag| %>
      <tr>
        <td><%= masters_tag.name%>
        <td><%= link_to 'Show', [:masters, masters_tag] %></td>
        <td><%= link_to 'Edit', edit_masters_tag_path(masters_tag) %></td>
        <td><%= link_to 'Destroy', [:masters,masters_tag], method: :delete, data: { confirm: 'Are you sure?' }%></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to ' お知らせタグ新規作成', new_masters_tag_path %>
