<p id="notice"><%= notice %></p>

<h1>Masters Chatroom Residences</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @masters_chatroom_residences.each do |masters_chatroom_residence| %>
      <tr>
        <td><%= link_to 'Show', masters_chatroom_residence %></td>
        <td><%= link_to 'Edit', edit_masters_chatroom_residence_path(masters_chatroom_residence) %></td>
        <td><%= link_to 'Destroy', masters_chatroom_residence, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Masters Chatroom Residence', new_masters_chatroom_residence_path %>
