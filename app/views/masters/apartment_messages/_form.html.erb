<%= form_with(model: masters_apartment_message, local: true) do |form| %>
  <% if masters_apartment_message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(masters_apartment_message.errors.count, "error") %> prohibited this masters_apartment_message from being saved:</h2>

      <ul>
        <% masters_apartment_message.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
